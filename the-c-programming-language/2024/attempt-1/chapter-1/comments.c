#include <stdio.h>

// Enter the main method
int main() {
    // Enter a comment
    printf("Print statement");

    /* This is a four loop
    it go loop loop loop */
    for (int i = 0; i < 10; ++i) {
        ;
         }
    // Return 0 to exit
    return 0;
}
